				let config = {
				   
					hot:$("#hot-controls-custom"),

					controls: [
					 "export",
					 "selector"
					]

				};
				  
				hot.handsontableControls($.extend({}, configuraciones, config));				